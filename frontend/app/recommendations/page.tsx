export default function RecommendationsPage() {
    return (
        <section className="rounded-[2rem] border border-black/5 bg-white/85 p-8 shadow-panel">
            <p className="text-sm uppercase tracking-[0.24em] text-accent">Screen 3</p>
            <h2 className="mt-2 text-3xl font-semibold">Recommendations</h2>
            <p className="mt-3 text-sm text-ink/70">Recommendation engine UI will connect to completed experiments in the next phase slice.</p>
        </section>
    );
}
